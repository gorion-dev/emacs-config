;;; org-images.el --- Image handling for Org mode

;; Automatically display images inline
(setq org-startup-with-inline-images t)

;; Allow custom scaling
(setq org-image-actual-width nil)

(provide 'org-images)
;;; org-images.el ends here
